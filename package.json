{
  "devDependencies": {
    "dotenv": "^16.4.5",
    "dotenv-cli": "^7.4.2",
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  },
  "dependencies": {
    "concurrently": "^9.1.0",
    "mysql2": "^3.11.4",
    "sequelize": "^6.37.5"
  },
  "scripts": {
    "start": "concurrently \"cd server && npm install && npm run start\" \"cd client && npm install && npm run start\""
  }
}
